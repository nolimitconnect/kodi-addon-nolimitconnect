sudo apt install -y build-essential autoconf automake libtool pkg-config

git clone https://github.com/xiph/opus.git
cd opus
git checkout v1.5.2   # latest stable as of 2026-1-17

./autogen.sh

./configure --disable-shared --enable-static 
